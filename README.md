# 🌌 Натальная Карта — Астрологический Калькулятор

Веб-приложение для расчёта натальных карт и транзитов с визуализацией и интерпретациями.

## 📋 Содержание

- [Возможности](#-возможности)
- [Технологии](#-технологии)
- [Установка](#-установка)
- [Запуск](#-запуск)
- [Структура проекта](#-структура-проекта)
- [API](#-api)
- [Как работают расчёты](#-как-работают-расчёты)
- [Деплой на сервер](#-деплой-на-сервер)

---

## ✨ Возможности

### Расчёты
- **Позиции планет**: Солнце, Луна, Меркурий, Венера, Марс, Юпитер, Сатурн, Уран, Нептун, Плутон
- **Дополнительные точки**: Лилит (Чёрная Луна), Северный узел, Южный узел
- **Куспиды домов**: Система Плацидус (12 домов)
- **ASC и MC**: Асцендент и Середина Неба
- **Аспекты**: Соединение, Секстиль, Квадрат, Тригон, Квинконс, Оппозиция
- **Транзиты**: Расчёт позиций планет на любую дату

### Визуализация
- Интерактивная карта неба (SVG)
- Цветовое кодирование знаков зодиака
- Линии аспектов с разными цветами и стилями
- Градусная шкала
- Маркеры ретроградности

### Интерпретации
- Тексты для планет в знаках
- Тексты для планет в домах
- Описания аспектов
- Матрица аспектов (натал × транзит)

### Экспорт
- Скачивание PDF-отчёта

---

## 🛠 Технологии

| Компонент | Технология |
|-----------|------------|
| Backend | Python 3.10+, FastAPI, Uvicorn |
| Расчёты | pyswisseph (Swiss Ephemeris) |
| Frontend | HTML5, CSS3, JavaScript (Vanilla) |
| Визуализация | SVG (кастомный NatalChart класс) |
| PDF | ReportLab |
| Геокодинг | Nominatim (OpenStreetMap) |
| Часовые пояса | pytz, timezonefinder |

---

## 📦 Установка

### Требования
- Python 3.10+
- pip

### Шаги

```bash
# 1. Клонировать репозиторий
git clone git@github.com:seomykyta-beep/natalnayakarta.git
cd natalnayakarta

# 2. Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Скачать файлы эфемерид (опционально, для высокой точности)
mkdir -p ephe
# Скачать sepl_18.se1, semo_18.se1, seas_18.se1 с https://www.astro.com/ftp/swisseph/ephe/
```

---

## 🚀 Запуск

### Локально (разработка)

```bash
source venv/bin/activate
python app.py
```

Приложение будет доступно по адресу: `http://localhost:8000`

### Продакшн

```bash
uvicorn app:app --host 0.0.0.0 --port 80
```

---

## 📁 Структура проекта

```
natalnayakarta/
├── app.py              # FastAPI приложение, роуты
├── engine.py           # Логика расчётов (Swiss Ephemeris)
├── generate_db.py      # Генерация базы текстов (утилита)
├── texts.json          # База интерпретаций
├── requirements.txt    # Зависимости Python
├── DejaVuSans.ttf      # Шрифт для PDF
├── static/
│   ├── chart.js        # Класс NatalChart для SVG визуализации
│   └── astrochart.min.js
├── templates/
│   └── index.html      # Главная страница
├── charts/             # Сохранённые SVG карты
├── reports/            # Сгенерированные PDF отчёты
└── ephe/               # Файлы эфемерид Swiss Ephemeris
```

---

## 🔌 API

### POST `/api/calculate`

Расчёт натальной карты и транзитов.

**Запрос:**
```json
{
  "name": "Имя",
  "year": 1992,
  "month": 7,
  "day": 24,
  "hour": 9,
  "minute": 10,
  "city": "Москва",
  "lat": 55.7558,
  "lon": 37.6173,
  "transit_year": 2025,
  "transit_month": 11,
  "transit_day": 27,
  "transit_hour": 12,
  "transit_minute": 0
}
```

**Ответ:**
```json
{
  "name": "Имя",
  "planets": [
    {
      "key": "Sun",
      "name": "Солнце",
      "icon": "☉",
      "sign": "Лев",
      "pos": 1.234,
      "abs_pos": 121.234,
      "house": 10,
      "is_retro": false,
      "text": "Интерпретация..."
    }
  ],
  "houses": [0, 30, 60, ...],
  "aspects": [...],
  "transits": [...],
  "transit_aspects": [...],
  "meta": {
    "city": "Москва",
    "dt": "24.07.1992 09:10"
  },
  "transit_meta": {
    "city": "Москва",
    "dt": "27.11.2025 12:00"
  }
}
```

### GET `/api/search_city?q=...`

Поиск города по названию (Nominatim).

### GET `/api/pdf?name=...`

Скачать PDF-отчёт.

---

## 🧮 Как работают расчёты

### 1. Ввод данных
Пользователь вводит дату, время рождения и город.

### 2. Геокодинг
По названию города получаем координаты (широта, долгота) и часовой пояс.

### 3. Юлианская дата
```
Дата + Время → UTC → Юлианская дата (JD)
```

### 4. Swiss Ephemeris
```python
import swisseph as swe

# Установка пути к эфемеридам
swe.set_ephe_path('/path/to/ephe')

# Расчёт позиции планеты
jd = swe.julday(year, month, day, hour + minute/60)
planet_pos = swe.calc_ut(jd, swe.SUN)[0][0]  # долгота в градусах
```

### 5. Дома (Плацидус)
```python
cusps, ascmc = swe.houses(jd, lat, lon, b'P')
# cusps[1:13] — куспиды 12 домов
# ascmc[0] — ASC, ascmc[1] — MC
```

### 6. Аспекты
```
|Планета1 - Планета2| = разница

0° ±8°   → Соединение
60° ±6°  → Секстиль
90° ±8°  → Квадрат
120° ±8° → Тригон
150° ±4° → Квинконс
180° ±8° → Оппозиция
```

### 7. Знак зодиака
```
долгота / 30 = индекс знака
долгота % 30 = градус в знаке
```

---

## 🖥 Деплой на сервер

### Ubuntu Server

```bash
# 1. Подключиться к серверу
ssh root@185.222.161.186

# 2. Установить зависимости
apt update && apt install -y python3 python3-pip python3-venv

# 3. Создать директорию
mkdir -p /opt/natal_chart
cd /opt/natal_chart

# 4. Скопировать файлы (или git clone)
git clone git@github.com:seomykyta-beep/natalnayakarta.git .

# 5. Создать venv и установить зависимости
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 6. Скачать эфемериды
mkdir -p ephe
# Скопировать sepl_18.se1, semo_18.se1, seas_18.se1

# 7. Открыть порт 80
ufw allow 80/tcp

# 8. Запустить
nohup python3 app.py > /var/log/natal_chart.log 2>&1 &
```

### Проверка
```bash
curl http://185.222.161.186/
```

---

## 📊 Точность расчётов

Расчёты совпадают с профессиональными астрологическими программами (GeoCult, Astro.com) благодаря:

1. **Swiss Ephemeris** — стандарт индустрии
2. **True Node** — истинный (не средний) лунный узел
3. **Delta T коррекция** — учёт разницы между UT и TT
4. **Файлы эфемерид** — точные данные JPL

---

## 📝 Лицензия

MIT License

---

## 👤 Автор

Разработано с использованием Claude AI (Anthropic)

**Сервер**: http://185.222.161.186

